# ── Go ────────────────────────────────────────────────────────────────────────
backend/bin/
backend/*.exe
backend/*.test
backend/*.out

# Runtime data directories (SQLite files, uploads, etc.)
data/
**/data/
backend/data/

# Go test / coverage
coverage.out
coverage.html

# ── SQLite databases ──────────────────────────────────────────────────────────
*.db
*.db-shm
*.db-wal

# ── Flutter / Dart ────────────────────────────────────────────────────────────
ui/.dart_tool/
ui/.flutter-plugins
ui/.flutter-plugins-dependencies
ui/build/
ui/.packages
ui/.pub/
ui/.pub-preload-cache/
# Platform targets not used in this project
ui/android/
ui/ios/
ui/linux/
ui/macos/
ui/windows/

# Dart generated files
**/generated_plugin_registrant.dart
**/GeneratedPluginRegistrant.*

# Flutter lock (regenerated from pubspec.yaml)
ui/pubspec.lock

# ── Chrome extension build ────────────────────────────────────────────────────
ui/chrome_extension/dist/
ui/chrome_extension/build/

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
.env.local
.env.*.local
*.env

# ── Buf / proto tooling ───────────────────────────────────────────────────────
.buf/

# ── Docker ────────────────────────────────────────────────────────────────────
# docker-compose.override.yaml is intentionally gitignored for local overrides
docker-compose.override.yaml

# ── Editor & OS artifacts ─────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

.idea/
.vscode/
*.swp
*.swo
*~
*.orig
*.bak

# JetBrains
*.iml
*.ipr
*.iws
.idea/

# ── Published / dist artifacts ────────────────────────────────────────────────
dist/
release/
out/
publish/
*.tar.gz
*.zip
*.dmg
*.pkg
*.deb
*.rpm
*.appimage

# Go release binaries
backend/plexreader
backend/server

# ── Temporary / misc ──────────────────────────────────────────────────────────
tmp/
temp/
*.tmp
*.log
