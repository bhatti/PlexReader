# PlexReader Backend Configuration
PLEXREADER_PORT=8080
PLEXREADER_DB_PATH=./data/plexreader.db
PLEXREADER_REFRESH_INTERVAL=15m
PLEXREADER_AUTH_ENABLED=false

# Optional: Auth configuration (when AUTH_ENABLED=true)
# PLEXREADER_AUTH_TOKEN=your-secret-token
